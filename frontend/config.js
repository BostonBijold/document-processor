// Simple config file (no build step, so no env vars in the browser) --
// point these at wherever Intake and Data are actually running.
window.APP_CONFIG = {
  INTAKE_URL: "http://localhost:8002",
  DATA_URL: "http://localhost:8001",
};
